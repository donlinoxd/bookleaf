Updated: May 29, 2026

- Role-based routing in client mode — librarian and admin accounts can manage the library (books, members, etc.) from any connected device on the same Wi-Fi, not just the main server tablet


DONE
- Bulk book import via Excel/CSV spreadsheet — type book details into a spreadsheet and upload as a file instead of adding one by one
- Able to enter accession, barcode, and shelf location on each copy when adding a new resource
- Add logout on client side
