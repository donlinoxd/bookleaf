Updated: May 23, 2026

- Gate Attendance Logs not sync with database or the entries didn't save it database?
- No React error boundaries anywhere
- IP input keyboard blocks `.` on Android
- 5 pre-existing TypeScript errors in `BorrowService.ts` (missing `renewal_count` in select casts) + 1 in `NotificationService.ts`
- Edit member and reset pin screens can't go back or click `cancel` and `save` buttons
- On first screen I clicked server, then close the app, then go back it then displays login screen. I can't login because it skipped the setup.
- Able to enter accession, barcode, and shelf location on each copy when adding new resource